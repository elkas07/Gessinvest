import { supabase } from './supabase.js'; async function load(){ const {data}=await supabase.from('projets').select('*'); const E=document.getElementById('cards-encours'); const A=document.getElementById('cards-atteint'); const V=document.getElementById('cards-avenir'); const card=p=>`<article class='card'><div class='card-body'><h3>${p.titre||'Projet'}</h3><div class='meta'><span>Objectif</span><strong>${p.objectif||0}</strong></div></div></article>`; (data||[]).forEach(p=>{const h=card(p); if(p.statut==='atteint')A.insertAdjacentHTML('beforeend',h); else if(p.statut==='avenir')V.insertAdjacentHTML('beforeend',h); else E.insertAdjacentHTML('beforeend',h);}); } load();