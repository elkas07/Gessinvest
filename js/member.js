import { supabase } from './supabase.js'; async function load(){ const {data}=await supabase.from('cotisations').select('montant,date_de_cotisation').order('date_de_cotisation',{ascending:false}); document.getElementById('body-cotis').innerHTML=(data||[]).map(r=>`<tr><td>${r.montant}</td><td>${new Date(r.date_de_cotisation).toLocaleDateString()}</td></tr>`).join('')||'<tr><td colspan="2">Aucune donnée.</td></tr>'; } load();